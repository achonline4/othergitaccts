#ifndef UTILS
#define UTILS

#include <limits.h>
enum TYPES{
	CHAR, U_CHAR, SHORT, U_SHORT, 
	INT, U_INT, LONG, U_LONG, DOUBLE, U_DOUBLE
};


int 
less_than_max(int type, int val)
{
	switch(type) {
		case CHAR:

			break;
		case U_CHAR:

			break;
		case INT:
			break;
	};
}

#endif
