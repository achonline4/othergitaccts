William Stallings


. Processor visible registers : not visible to the assembly programmer.
	-> PC: Program counter, contains the address of the next instrcution to be fetched
	-> IR: Contains the most recently fetched instruction.

Control and status registers: Used by the processor to control the operation
of the processor and by privileged OS routines to control the execution of
programs.

User visible registers:
